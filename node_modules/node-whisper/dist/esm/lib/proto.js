import fs from 'node:fs/promises';
export const getProto = (key, value) => {
    return {
        file: value,
        getContent: async () => {
            const content = await fs.readFile(value, { encoding: 'utf8' });
            if (key === 'json')
                return JSON.parse(content);
            return content;
        },
    };
};
//# sourceMappingURL=proto.js.map